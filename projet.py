## Archive principal - exec du projet

class car:
    def __init__(self, init_x: float, init_y: float, init_yaw: float, px: float, py: float, pyaw: float, delta_time: float):
        self.x = init_x
        self.y = init_y
        self.yaw = yaw
        self.x